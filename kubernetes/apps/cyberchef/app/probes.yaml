---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: cyberchef
  namespace: cyberchef
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.blackbox-exporter.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - "https://chef.${SECRET_DEFAULT_DOMAIN}"
